
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Update This Flight</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <form autocomplete="on" id='flight-update-form'>
          <input type="hidden" name=flight[id] value={{flight.id}}>
          Flight Type:<input class="flight-input" type="string" name="flight[flightType]" required value='{{flight.flight_type}}'><br>
          Date:<input class="flight-input" type="date" name="flight[date]" required autocomplete="off" value={{flight.date}}><br>
          Time: <input class="flight-input" name="flight[time]" required autocomplete="off" value={{flight.time}}><br>
          Location: <input class="flight-input" type="text" required name="flight[launch]" value={{flight.launch}}><br>
          Landing Zone: <input class="flight-input" type="text" required name="flight[lz]" value={{flight.lz}}><br>
          Duration: <input class="flight-input" type="number" required step="0.01" name="flight[duration]"  value={{flight.duration}} autocomplete="off">hrs.
          <!-- <input name="min" class="flight-input" type="number" min="0" max="60" placeholder="mins" autocomplete="off"> -->
          <br>
          Distance: <input class="flight-input" required name="flight[distance]" placeholder='km' value={{flight.distance}}><br>
          Glider: <input class="flight-input" type="text" required name="flight[wing]" placeholder="glider" value={{flight.wing}} ><br>
          Notes: <br>
          <textarea class="flight-input" rows="4" cols="50" required name="flight[notes]" form="new-flight-form" placeholder="enter notes here..." autocomplete="off" >{{flight.notes}}</textarea>
          <button type="submit">Save Changes</button>
        </form>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <!-- <button id='edit-save-button' type="button" class="btn btn-primary">Save changes</button> -->>
      </div>
